const currentSpeed = 90
const speedLimit = 80

if (currentSpeed > speedLimit) {
    console.log('You\'re too fast!')
} else if (currentSpeed === speedLimit) {
    console.log('Careful!')
} else {
    console.log('Great!')
}