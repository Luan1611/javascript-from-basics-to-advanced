const names = ['Maria', 'Carlos', 'José', 'Luan', 'Helena']
const myName = 'Luan'

if (names.includes(myName)) {
    console.log(`'${myName}' existe no array.`)
} else {
    console.log(`'${myName}' não existe no array.`)
}